# nullofexile free demo

a free demo for nullofexile — modify your path of exile experience with ease.

---

## ⚙️ instructions

1. download and install [visualggpk3](https://github.com/aianlinb/LibGGPK3/releases/tag/v2.6.10).
2. extract the `shaders` folder from your path of exile directory.
3. download and install `nullofexile.exe` from the [releases](https://github.com/yourrepo/releases) page.
4. launch `nullofexile.exe` and select the path to the extracted `shaders` folder.
5. the app will automatically modify the contents of the `shaders` folder.
6. once the log in the app shows **"done!"**, compress the modified `shaders` folder into a `.zip` file.
7. open **visualggpk3**, click **patch**, and select the `.zip` file you created.

---

## 📁 example folder structure

